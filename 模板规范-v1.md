# 钻石拼贴 · 模板规范 v1(参考图 → 可玩模板的通用规则)

> 2026-09-23 定稿 | 回答"如何把参考图统一处理成游戏里可玩的模板"

## 一、模板的数据格式(游戏唯一消费格式)

一个模板 = 一个 JSON 文件,存放 `模板库/<id>.json`:

```json
{
  "id": "clover",
  "name": "clover",
  "canvas": 360,
  "colorHint": "green",
  "source": "07-四叶草.jpg",
  "spots": [ {"x": 118.5, "y": 68.0, "r": 30, "tier": "L"}, ... ],
  "total": 27
}
```

- `spots` 即框架内的密铺点位:玩家逐颗往点位上贴钻
- `tier`:L/M/S 三档大小(半径 30/22/15,逻辑坐标),决定用大钻还是填缝小钻
- `colorHint`:推荐色系包(只做引导,玩家可无视——自由配色是产品灵魂)
- 游戏运行时:模板 JSON × 色系包(见下)→ 每个点位随机抽同色系素材图贴上

## 二、素材格式(单颗钻/装饰)

- 全部为透明底 PNG,存 `素材库/钻石素材-单颗/`
- `模板库/gems-manifest.json` 是素材总清单:228 颗,file + color + 尺寸
- **色系包**(玩家选择的单位)= 同色系深浅混合:

| 包 | 颜色来源 | 数量 |
|---|---|---|
| purple | purple + lavender | 18 |
| blue | blue + sky | 33 |
| gold | gold + yellow | 18 |
| pink | pink + magenta + peach | 70 |
| green | green + lime | 26 |
| red | red | 18 |
| silver | silver + white | 41 |

- 贴钻规则:玩家选中包后,每个点位按 tier 缩放素材图(直径=tier 半径×2),包内随机抽一张
- 装饰元素(像素图标/爱心/果冻按钮)存 `素材库/可爱元素-单颗/`,进界面不进点位

## 三、参考图 → 模板:三档生成规则

| 档 | 输入 | 方式 | 质量 | 单个耗时 |
|---|---|---|---|---|
| **A 几何生成** | 形状创意(不依赖图) | 数学基元拼轮廓 + 密铺采样(管线同款) | 最高,100% 干净 | 15~30 分钟 |
| **B 白底插画** | 白底/纯底插画图 | 全自动:抠图→主体→闭运算→密铺 | 高,基本免修 | 1 分钟 |
| **C 照片/截图** | 实拍图(碎片化钻面) | 半自动:ROI+主体框标注 → 自动密铺 → 预览人工微调 | 中,需人工补点 | 5~10 分钟 |

管线脚本:`scripts/pipeline_ref_to_template.py`(已跑通 clover / popcorn 两个 demo)

### C 档人工标注只有两个框(各几秒):
1. `roi`(画纸框):截掉 App 边框/字幕等
2. `subject`(主体框):只保留主体形状,剔除周围散钻

### 已验证的经验值:
- 背景色取**画面最亮 12% 像素的中位数**(不要取四角——截图四角常是 App UI)
- 碎片化钻面必须做**闭运算**(MaxFilter 15 → MinFilter 15)合并缝隙,否则大钻放不进去
- 密铺:三层 dart-throwing(L→M→S)+ 2 轮松弛,点间距 = r1+r2-3(允许轻微相切)
- 目标颗数 20~80:四叶草 15~27、爆米花桶 19,均在区间;偏稀属 C 档正常,人工补点

## 四、模板生产流程(后续通用)

```
选题(参考图/创意) → 选档(A/B/C) → 生成 JSON → 预览图人工确认
   → (C 档微调点位) → 入库 模板库/ → 游戏自动加载
```

- 首发模板清单(对应 14 张参考图):谱号、苹果、雨云、圣诞树、葡萄串、蛋糕、鸡尾酒、四叶草、魔法棒、蝴蝶葡萄、冰淇淋、雨伞、海豚、爆米花桶
- 每张参考图先走 B/C 档试产;质量不达标的转 A 档几何重做
